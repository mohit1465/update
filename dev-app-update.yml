owner: mohit1465
repo: update
provider: github
publishAutoUpdate: true
updaterCacheDirName: "electron-auto-update-app"
updaterFullChangelog: true

# Update configuration to match the new naming pattern
updaterConfig: {
  "provider": "github",
  "owner": "mohit1465",
  "repo": "update",
  "releaseType": "release"
}

# Set the correct artifact name pattern
artifactName: "electron-auto-update-app.Setup.${version}.exe"
